/// <reference types="svelte" />
/// <reference types="vite/client" />

// Stamped by vite.config.ts's `define` at build time (git short SHA, or 'dev').
// Guarded with `typeof` at every use site: the vitest projects and svelte-check
// don't run vite's define transform.
declare const __BUILD_ID__: string
