---
id: T-009-03-01
story: S-009-03
title: tenpai-riichi-prompt
type: task
status: open
priority: high
phase: ready
depends_on: [T-009-01-01]
---

## Context

The definition-of-done moment: when riichi is offered, the view prompts "you're tenpai —
declare riichi?" with the stakes explained in one line each (hand locks, 1000-point stick,
riichi yaku + uradora chance), through the existing prompt/console seam. Pre-tenpai, a
subtle shanten hint ("2 away from tenpai") reads the core shanten module. Decline keeps
playing; the prompt returns at later legal moments.

_Advances: P2, P1_

## Acceptance Criteria

- [ ] SSR/drive tests: the prompt renders exactly when legalActions offers riichi, the
  stakes text is present, declining folds the plain discard, accepting folds the riichi
  action, and the shanten hint tracks the fold-derived count (no engine logic in the
  view).
