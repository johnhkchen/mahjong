---
id: T-001-01-02
story: S-001-01
title: vite-svelte-vitest-scaffold-and-justfile
type: task
status: open
priority: critical
phase: ready
depends_on: [T-001-01-01]
---

## Context

Stand up the Vite + Svelte 5 + TypeScript + vitest + vite-plugin-singlefile scaffold with empty src/core/ and src/app/ boundaries, driven by justfile dev/test/check/build recipes — the delivery spine every rules epic lands on.

_Advances: P3, P5, E-001_

## Acceptance Criteria

- [ ] From a fresh clone: `just check` (svelte-check + tsc) exits clean, `just test` runs vitest successfully, `just build` emits dist/index.html via vite-plugin-singlefile, and `just dev` serves a page — all through the flox toolchain.
