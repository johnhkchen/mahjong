---
id: T-009-02-02
story: S-009-02
title: riichi-dynamics-suite
type: task
status: open
priority: medium
phase: ready
depends_on: [T-009-02-01, T-009-01-04]
---

## Context

Close the loop at game level: seeded multi-hand AI-vs-AI games where bots declare riichi
fold deterministically end to end, with the stick pot conserved across hands and
ryuukyoku carries — the scored-and-staked extension of the E-008 dynamics suite.

_Advances: P5_

## Acceptance Criteria

- [ ] Multi-hand botted games across seeds: byte-identical replays, points + pot conserved
  at every boundary including pot carries, riichi declarations occur in the sample (the
  suite asserts non-zero incidence so the property isn't vacuous), and no stalls.

## Repair note (overseer, 2026-07-04 — read before resuming)

A prior session committed partial work ("Fix game-level pot conservation and assert riichi
incidence", 751ec/751c34a-era commits) and died leaving `just test` RED with 5 failures,
all in this ticket's conservation/dynamics remit. Reconcile, don't revert:

1. `settlement.property.test.ts` — "four deltas sum to zero": hand-level settlement now
   has riichi-stick flows; the property needs the pot term (deltas + pot movement = 0, or
   points + pot invariant), consistent with how game-level conservation was fixed.
2. `selfplay.test.ts` seeds 25 (menzen-tsumo) and 13 (houtei ron) — frozen named-seed
   anchors: bots now declare riichi, trajectories legitimately changed. Re-mine/re-freeze
   the anchors for the new behavior (verify the new trajectory is sane, then re-pin).
3. `drive.test.ts` — "a BOT rons the player": same riichi-behavior staleness; re-anchor.
4. `app.controls.svelte.test.ts` — "resets scores to 25000 each": new-game reset must
   also clear/reset the riichi pot; check newGame()/foldGame boot state.

Done still means: `just test` fully green, then this ticket's own criteria above.
