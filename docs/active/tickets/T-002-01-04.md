---
id: T-002-01-04
story: S-002-01
title: hand-record-fold-entrypoint
type: task
status: done
priority: high
phase: done
depends_on: [T-002-01-01, T-002-01-03]
---

## Context

Make 'a hand is its record' running code: define the hand-record type (seed + ordered action log) and the fold entrypoint (record in → table state out) as core's public contract that every later epic reads.

_Advances: P5, P1_

## Acceptance Criteria

- [ ] Folding a record with an empty action log yields the table state (live/dead wall, dora indicator + mapped dora, four hands, seats); property tests 'same seed → identical deal' and 'same record → same folded state (deep-equal across repeated folds)' pass under `just test`; the fold and record types are exported from src/core/index.ts.
