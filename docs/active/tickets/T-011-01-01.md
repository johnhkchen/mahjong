---
id: T-011-01-01
story: S-011-01
title: claim-window-interaction-repro
type: task
status: open
priority: critical
phase: done
depends_on: []
---

## Context

Script the owner's playtest sequences as jsdom interaction tests before any fix: (a) a
mixed window where the player taps chi/pon and a bot's higher-priority claim wins the
arbitration — assert the tap is currently discarded with no visible outcome and that a
similar window can open on the winner's next discard; (b) consecutive claim windows on
the same kind rendering as an apparently-identical prompt; (c) regression for the houtei
`dismissed` reset at hand boundaries (commit 3bcf9d3). Characterization first — these
tests pin the defect and then flip to pinning the fix.

_Advances: P2, P5_

## Acceptance Criteria

- [ ] A jsdom suite drives the three sequences through real taps on mined seeds (or
  hand-built records) and documents current behavior with explicit `// DEFECT:` markers
  where the fix tickets will flip assertions; the houtei-reset regression passes
  already. `just test` green.
