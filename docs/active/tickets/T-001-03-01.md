---
id: T-001-03-01
story: S-001-03
title: svelte-empty-table-view
type: task
status: done
priority: high
phase: done
depends_on: [T-001-02-02]
---

## Context

Thin src/app/ Svelte 5 view (runes $state/$derived) that renders an empty mobile-first table from a wall dealt by the seeded engine — proving the core→app boundary in the one direction the architecture allows.

_Advances: P3, P4, E-001_

## Acceptance Criteria

- [ ] `just dev` renders an empty table whose displayed wall/tile count is derived from a src/core/ seeded wall build (not hardcoded); `just check` is clean; import direction is app→core only, with no app or DOM imports appearing in src/core/.
