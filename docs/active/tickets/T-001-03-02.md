---
id: T-001-03-02
story: S-001-03
title: single-file-offline-build-and-deploy-recipe
type: task
status: done
priority: high
phase: done
depends_on: [T-001-03-01]
---

## Context

Prove the single-file compile target end-to-end — one self-contained dist/index.html that renders the empty table with no network — and add the `just deploy` recipe targeting Cloudflare static (pipeline wiring stays out of scope per the epic).

_Advances: P3, E-001_

## Acceptance Criteria

- [ ] `just build` emits exactly one dist/index.html with no external script/link/img references (all JS/CSS inlined), and opening it from the filesystem with the network disabled renders the empty table; a `just deploy` recipe exists and validates (e.g. wrangler dry-run) without requiring the live pipeline.
